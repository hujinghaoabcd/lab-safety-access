name: Retired phase 2 lockfile helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "This one-time lockfile helper is retired and can be deleted."
